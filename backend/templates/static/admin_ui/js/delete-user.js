document.addEventListener('DOMContentLoaded',()=>{document.addEventListener('click',(e)=>{const b=e.target.closest('.users-row-actions .is-delete');if(!b)return;const row=b.closest('tr'),u=row.children[1].textContent.trim(),role=row.children[6].textContent.trim();const m=document.createElement('div');m.className='delete-modal';m.innerHTML=`<div class="delete-dialog"><h2>⚠ Delete User</h2><p>Are you sure you want to delete <b>${u}</b>?<br>This action will permanently remove the user with the role of <b>${role.toUpperCase()}</b> from the system. This cannot be undone.</p><label>Username: <input placeholder="Enter username to confirm deletion."></label><aside><b>Warning!</b><br>Please be careful, this operation can not be rolled back.</aside><footer><button>Cancel</button><button disabled>Delete</button></footer></div>`;document.body.append(m);const [cancel,del]=m.querySelectorAll('footer button'),i=m.querySelector('input');i.oninput=()=>del.disabled=i.value!==u;cancel.onclick=()=>m.remove();del.onclick=()=>{row.remove();m.remove()};m.onclick=x=>{if(x.target===m)m.remove()}})})
