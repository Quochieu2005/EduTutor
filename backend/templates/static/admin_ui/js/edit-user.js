document.addEventListener('DOMContentLoaded', () => { const entityLabel = document.body.dataset.entityLabel || 'User'; document.addEventListener('click', (event) => { const button = event.target.closest('.users-row-actions button:not(.is-delete)'); if (!button) return; const row = button.closest('tr'); const modal = document.querySelector('.add-user-modal'); if (!row || !modal) return; const cells = row.querySelectorAll('td'); modal.querySelector('h2').textContent = `Edit ${entityLabel}`; modal.querySelector('.add-user-dialog > p').textContent = `Update ${entityLabel.toLowerCase()} information here. Click save when you're done.`; const inputs = modal.querySelectorAll('input:not([type="file"])'); const name = cells[2].textContent.trim().split(' '); inputs[0].value = name.shift() || ''; inputs[1].value = name.join(' '); inputs[2].value = cells[1].textContent.trim(); inputs[3].value = cells[3].textContent.trim(); inputs[4].value = cells[4].textContent.trim(); modal.hidden = false; }); });
