document.addEventListener('DOMContentLoaded', () => {
  const filters = document.querySelector('.users-toolbar__filters'); const menus = document.querySelectorAll('.users-status-popover, .users-role-popover');
  const render = () => { filters.querySelector('.users-selected-filters')?.remove(); const selected = [...menus].flatMap((menu) => [...menu.querySelectorAll('input:checked')].map((input) => input.closest('label').querySelector('span').textContent)); if (!selected.length) return; const group = document.createElement('div'); group.className = 'users-selected-filters'; group.innerHTML = `${selected.map((name) => `<span>${name}</span>`).join('')}<button type="button" data-reset>Reset</button><button type="button" data-reset aria-label="Clear filters"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 6 6 18M6 6l12 12"></path></svg></button>`; filters.appendChild(group); group.querySelectorAll('[data-reset]').forEach((button) => button.onclick = () => { menus.forEach((menu) => menu.querySelectorAll('input').forEach((input) => input.checked = false)); render(); }); };
  menus.forEach((menu) => menu.addEventListener('change', render));
});
