document.addEventListener('DOMContentLoaded', () => {
  const form = document.querySelector('.add-user-dialog form'); if (!form) return;
  form.insertAdjacentHTML('afterbegin', '<label>Profile Image</label><input type="file" accept="image/*" aria-label="Upload profile image">');
  const imageInput = form.querySelector('input[type="file"]'); imageInput.id = 'profile-image-input'; imageInput.className = 'profile-image-input'; imageInput.insertAdjacentHTML('afterend', '<label class="profile-image-upload" for="profile-image-input"><span class="profile-image-preview">+</span><b>Choose image</b><em>No image selected</em></label>'); imageInput.addEventListener('change', () => { const upload = form.querySelector('.profile-image-upload'); const preview = upload.querySelector('.profile-image-preview'); upload.querySelector('em').textContent = imageInput.files[0]?.name || 'No image selected'; if (imageInput.files[0]) { const reader = new FileReader(); reader.onload = () => { preview.style.backgroundImage = `url(${reader.result})`; preview.textContent = ''; }; reader.readAsDataURL(imageInput.files[0]); } });
  const eye = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7S2 12 2 12"></path><circle cx="12" cy="12" r="3"></circle></svg>';
  form.querySelectorAll('input[type="password"]').forEach((input) => { const wrap = document.createElement('div'); wrap.className = 'password-field'; const button = document.createElement('button'); button.type = 'button'; button.className = 'password-toggle'; button.innerHTML = eye; button.addEventListener('click', () => { const visible = input.type === 'password'; input.type = visible ? 'text' : 'password'; button.innerHTML = visible ? '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m3 3 18 18"></path><path d="M10.6 10.6a2 2 0 0 0 2.8 2.8"></path><path d="M9.9 4.2A11 11 0 0 1 12 4c6.5 0 10 8 10 8a18 18 0 0 1-3.2 4.2M6.2 6.2C3.7 8 2 12 2 12s3.5 8 10 8a10 10 0 0 0 4.2-.9"></path></svg>' : eye; }); input.replaceWith(wrap); wrap.append(input, button); });
});
