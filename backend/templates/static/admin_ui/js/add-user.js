document.addEventListener('DOMContentLoaded', () => {
  const trigger = document.querySelector('.users-button--primary'); if (!trigger) return;
  const modal = document.createElement('div'); modal.className = 'add-user-modal'; modal.hidden = true;
  modal.innerHTML = '<div class="add-user-dialog" role="dialog"><button class="add-user-close" aria-label="Close">×</button><h2>Add New User</h2><p>Create new user here. Click save when you\'re done.</p><form><label>First Name<input placeholder="John"></label><label>Last Name<input placeholder="Doe"></label><label>Username<input placeholder="john_doe"></label><label>Email<input type="email" placeholder="john.doe@gmail.com"></label><label>Phone Number<input placeholder="+123456789"></label><label>Role<select><option>Select a role</option><option>Admin</option><option>Manager</option></select></label><label>Password<input type="password" placeholder="e.g., S3cur3P@ssw0rd"></label><label>Confirm Password<input type="password" placeholder="e.g., S3cur3P@ssw0rd"></label><button class="add-user-save" type="submit">Save changes</button></form></div>';
  document.body.appendChild(modal); const close = () => modal.hidden = true; trigger.addEventListener('click', () => modal.hidden = false); modal.querySelector('.add-user-close').onclick = close; modal.addEventListener('click', (event) => { if (event.target === modal) close(); }); modal.querySelector('form').addEventListener('submit', (event) => { event.preventDefault(); close(); });
  document.addEventListener('keydown', (event) => { if (event.key === 'Escape') close(); });
});
